{
  "name": "@coin-district/coindistrict-core",
  "version": "0.25.2",
  "private": false,
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "description": "Foundry-based ERC-3643 smart contracts for Coin District",
  "license": "GPL-3.0",
  "scripts": {
    "build": "forge build && bash scripts/generate-abis.sh",
    "build:abis": "bash scripts/generate-abis.sh",
    "prepublishOnly": "pnpm build",
    "test": "forge test",
    "lint": "forge lint",
    "prepare": "husky"
  },
  "files": [
    "contracts",
    "abis",
    "foundry.toml",
    "remappings.txt",
    "foundry.lock",
    "config",
    "out"
  ],
  "devDependencies": {
    "@chainlink/contracts": "^1.5.0",
    "husky": "^9.0.0"
  }
}
